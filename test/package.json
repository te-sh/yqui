{
  "name": "yqui-e2e-test",
  "version": "0.1.0",
  "license": "AFL-2.0",
  "scripts": {
    "test": "jest --config config.json --runInBand --silent"
  },
  "devDependencies": {
    "expect-more-jest": "^5.2.2",
    "jest": "^26.6.3",
    "puppeteer": "^7.1.0"
  }
}
